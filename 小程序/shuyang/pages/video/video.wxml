<view class="container" hidden="{{!hidden}}">
  <scroll-view
    scroll-y="true"
    style="height:{{scrollH}}px"
    bindscrolltoupper="refresh"
    bindscrolltolower="loadMore">
    <swiper class="banners"
      autoplay="{{autoplay}}"
      indicator-dots="{{indicatorDots}}"
      interval="{{interval}}"
      duration="{{duration}}"
      circular="{{circular}}"
      indicator-color="#FFF">
      <block wx:for="{{hots}}" wx:key="key">
          <swiper-item class="banner">
              <image mode="aspectFill" src="https://img.05273.cn/images/videobg.jpg" class="banner-image" width="100%" height="100%"/>
              <view class="banner-title">
                <text>{{item.title}}</text>
              </view>
          </swiper-item>
      </block>
    </swiper>
    <view class="news-item-container">
      <view hidden="{{!hidden}}" class="sub-title">
        <text>{{subTitle}}</text>
      </view>
      <block wx:for="{{video}}" wx:key="key">
        <navigator wx:if="{{item.id}}" url="../player/player?catid={{item.catid}}&id={{item.id}}">
          <view class="news-item">
            <view class="news-item-left">
              <image mode="aspectFill" src="https://img.05273.cn/images/videobg.jpg" class="news-image"/>
            </view>
            <view class="news-item-right">
              <text class="news-item-title">{{item.title}}</text>
              <text class="news-item-description">{{item.description}}</text>
              <view class="news-item-info">
                <text class="news-item-category">{{item.catname}}</text>
                <text class="news-item-pubdate">{{item.pubdate}}</text>
              </view>
            </view>
          </view>
        </navigator>
      </block>
    </view>
  </scroll-view>
  <view class="weui-loadmore" hidden="{{loading}}">
      <view class="weui-loading"></view>
      <view class="weui-loadmore__tips">数据加载中...</view>
  </view>
</view>
<loading class="loading" hidden="{{hidden}}">加载中...</loading>
